// 2 Чему будут равны переменные x и a в примере ниже? Написать почему так произошло
// (описать последовательность действий).
// let a = 2;
// let x = 1 + (a *= 2);

// объявляется переменная a и ей присваивается значение 2. далее объявляется переменная х, которой присваивается
// следующее значение - заново переопределяется а, теперь она равна 4, далее к этому значению прибавляется 1. Итог x = 5